// random.c
// Jon Atkinson, Sep 24

#include <stdio.h>

int main(void) {
    // we don't usually recommend making your strings this long but if you do
    // need to wrap them, this is the correct method
    printf("this is a random directory, it contains random things that don't \
fit anywhere else in this repo (i.e. tuts/slides)\n");

    return 0;
}

