// Basic Hello, World! progam

#include <stdio.h>

int main(void) {
  // This prints "Hello, World!" to the terminal, and the next terminal
  // commend is printed on the line below it, instead of the on the same
  // line.
  // What does this imply about what '\n' does?
  printf("Hello, World!\n");

  return 0;
}
